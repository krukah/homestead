# Secrets — these should never have a path here in the first place,
# but belt + suspenders.
*.local
.env
.env.*
**/credentials
**/credentials.json
**/*token*
**/*secret*
**/*.pem
**/id_rsa*
**/id_ed25519*

# Junk
.DS_Store
**/.tmp*

# Tool-managed metadata that bakes in absolute paths and rewrites itself.
home/.config/uv/uv-receipt.json
